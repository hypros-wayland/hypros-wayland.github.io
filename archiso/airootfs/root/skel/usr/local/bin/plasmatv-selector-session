#!/usr/bin/env bash
# The whole "session" for selector-user is just this app, fullscreen,
# nothing else running. When it exits (a login was handed off, or SDDM
# is about to restart anyway) there's nothing else to clean up.
exec /usr/bin/electron /opt/plasmatv-apps/user-selector
